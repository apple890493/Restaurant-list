<h1 class="mb-1 text-align restaurant-show-title">編輯餐廳 <i class="fas fa-pencil-alt"></i></h1>
<div class="container" id="table">
  <div class="row">
    <div class="col-12 col-md-10 col-xl-8 mx-auto">
      <form action="/restaurants/{{restaurant._id}}/edit" method="POST">

        <div class="form-group form-row">
          <label for="name" class="col-md-2 col-form-label">餐廳名稱:</label>
          <div class="col-md-10">
            <input type="text" name="name" class="form-control mb-1" id="name" placeholder="Name"
              value="{{restaurant.name}}">
            <input type="text" name="name_en" class="form-control" id="name_en" placeholder="Name_en"
              value="{{restaurant.name_en}}">
          </div>
        </div>

        <div class="form-group form-row">
          <label for="category" class="col-md-2 col-form-label">餐廳類別:</label>
          <div class="col-md-10">
            <input type="text" name="category" list="categoryType" placeholder="EX:西式料理"
              value="{{restaurant.category}}">
            <datalist id="categoryType">
              <option value="西式料理">西式料理</option>
              <option value="日式料理">日式料理</option>
              <option value="義式餐廳">義式餐廳</option>
              <option value="中式餐廳">中式餐廳</option>
              <option value="泰式料理">泰式料理</option>
              <option value="異國料理">中東料理</option>
              <option value="異國料理">中東料理</option>
              <option value="其他">其他</option>
            </datalist>
          </div>
        </div>

        <div class="form-group row">
          <label for="location" class="col-md-2 col-form-label">餐廳地址：</label>
          <div class="col-md-10">
            <input type="text" class="form-control mb-1" id="location" name="location" placeholder="Address"
              value="{{restaurant.location}}">
            <input type="text" class="form-control" id="google_map" name="google_map" placeholder="https://goo.gl/maps/"
              value="{{restaurant.google_map}}">
          </div>
        </div>

        <div class="form-group row">
          <label for="phone" class="col-md-2 col-form-label">餐廳電話：</label>
          <div class="col-md-10">
            <input type="text" class="form-control" id="phone" name="phone" placeholder="Phone"
              value="{{restaurant.phone}}">
          </div>
        </div>

        <div class="form-group row">
          <label for="image" class="col-sm-2 col-form-label">餐廳圖片：</label>
          <div class="col-sm-10">
            <input type="text" class="form-control" id="image" name="image" placeholder="http://"
              value="{{restaurant.image}}">
          </div>
        </div>

        <div class="form-group row">
          <label for="description" class="col-sm-2 col-form-label">餐廳簡介：</label>
          <div class="col-sm-10">
            <textarea class="form-control" id="description" name="description" rows="3"
              placeholder="填寫評論...">{{restaurant.description}}</textarea>
          </div>
        </div>

        <div class="form-group row">
          <label for="rating" class="col-md-2 col-form-label">餐廳評價：</label>
          <div class="col-md-10 d-flex align-items-center">
            <div class="form-check form-check-inline">
              <input type="text" class="form-control" id="rating" name="rating" min="0" max="5" placeholder="EX:4.2"
                value="{{restaurant.rating}}">
            </div>
          </div>
        </div>
        {{#if dataError}}
        <div class="alert alert-danger row mx-5 justify-content-center w-100" role="alert">
          必要參數不能為空，已回復上次儲存資料!!
        </div>
        {{/if}}

        <div class="form-group row justify-content-end">
          <button type="submit" class="btn btn-info mb-3 mr-2" id="saveBtn">Save</button>
          <a href="/"><button type="button" class="btn btn-dark">Back</button></a>
        </div>
      </form>
    </div>
  </div>
</div>